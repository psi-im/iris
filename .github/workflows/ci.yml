name: Build and test

on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: iris-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  QT_VERSION: 6.10.2
  QCA_TAG: v3.0.1
  ANDROID_NDK_VERSION: r27c
  ANDROID_API: 28

jobs:
  linux:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: qt5-qca2
            qt_major: 5
            qca: qca2
            deps: qtbase5-dev libqca-qt5-2-dev libb2-dev
            options: -DIRIS_SYSTEM_QCA=2 -DIRIS_BUNDLED_QCA=OFF
          - name: qt6-qca2
            qt_major: 6
            qca: qca2
            deps: qt6-base-dev libqca-qt6-dev
            options: -DUSE_QT6=ON -DIRIS_SYSTEM_QCA=2 -DIRIS_BUNDLED_QCA=OFF
          - name: qt6-qca3
            qt_major: 6
            qca: qca3
            deps: qt6-base-dev
            options: -DUSE_QT6=ON -DIRIS_SYSTEM_QCA=3 -DIRIS_BUNDLED_QCA=OFF
          - name: qt6-qca3-omemo
            qt_major: 6
            qca: qca3
            deps: qt6-base-dev libomemo-c-dev
            options: -DUSE_QT6=ON -DIRIS_SYSTEM_QCA=3 -DIRIS_BUNDLED_QCA=OFF -DIRIS_ENABLE_OMEMO=ON -DIRIS_BUNDLED_OMEMO_C=OFF
          - name: qt6-bundled-qca3
            qt_major: 6
            qca: bundled
            deps: qt6-base-dev libssl-dev
            options: -DUSE_QT6=ON -DIRIS_BUNDLED_QCA=ON -DIRIS_BUNDLED_QCA_GIT_TAG=v3.0.1

    name: Linux (${{ matrix.name }})
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build zlib1g-dev libusrsctp-dev ${{ matrix.deps }}

      - name: Install QCA3 release packages
        if: matrix.qca == 'qca3'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir qca-release
          gh release download "$QCA_TAG" --repo psi-im/qca \
            -p 'qca3-deb-ubuntu-24.04.zip' -O qca3.zip
          (cd qca-release && cmake -E tar xvf ../qca3.zip)
          test -n "$(find qca-release -type f -name '*.deb' -print -quit)"
          sudo apt-get install -y ./qca-release/*.deb

      - name: Configure
        shell: bash
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX="$PWD/sdk" \
            -DIRIS_BUNDLED_USRSCTP=OFF \
            ${{ matrix.options }}

      - name: Build and install
        shell: bash
        run: |
          cmake --build build --parallel
          cmake --install build

      - name: Compile installed CMake consumer
        shell: bash
        run: |
          mkdir consumer
          cat > consumer/CMakeLists.txt <<'CMAKE'
          cmake_minimum_required(VERSION 3.16)
          project(iris_consumer LANGUAGES CXX)
          find_package(Iris CONFIG REQUIRED)
          add_executable(iris-consumer main.cpp)
          target_link_libraries(iris-consumer PRIVATE Iris::Iris)
          CMAKE
          cat > consumer/main.cpp <<'CPP'
          #include <iris/jid/jid.h>
          int main()
          {
              XMPP::Jid jid(QStringLiteral("ci@example.org"));
              return jid.isValid() ? 0 : 1;
          }
          CPP
          cmake -S consumer -B consumer-build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$PWD/sdk"
          cmake --build consumer-build
          LD_LIBRARY_PATH="$PWD/sdk/lib:$PWD/sdk/lib/x86_64-linux-gnu:${LD_LIBRARY_PATH:-}" \
            ./consumer-build/iris-consumer

  desktop:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows-x64
          - os: macos-15
            platform: macos-arm64
          - os: macos-15-intel
            platform: macos-x86_64
    runs-on: ${{ matrix.os }}
    name: Desktop (${{ matrix.platform }})
    steps:
      - uses: actions/checkout@v4

      - name: Build and install Iris
        uses: ./.github/actions/build-iris-desktop
        with:
          qt-version: ${{ env.QT_VERSION }}
          platform: ${{ matrix.platform }}
          qca-tag: ${{ env.QCA_TAG }}
          install-prefix: ${{ github.workspace }}/sdk

      - name: Compile installed CMake consumer
        shell: bash
        run: |
          mkdir consumer
          cat > consumer/CMakeLists.txt <<'CMAKE'
          cmake_minimum_required(VERSION 3.16)
          project(iris_consumer LANGUAGES CXX)
          find_package(Iris CONFIG REQUIRED)
          add_executable(iris-consumer main.cpp)
          target_link_libraries(iris-consumer PRIVATE Iris::Iris)
          CMAKE
          cat > consumer/main.cpp <<'CPP'
          #include <iris/jid/jid.h>
          int main()
          {
              XMPP::Jid jid(QStringLiteral("ci@example.org"));
              return jid.isValid() ? 0 : 1;
          }
          CPP
          if [ "$RUNNER_OS" = "Windows" ]; then
            cmake -S consumer -B consumer-build -G Ninja \
              -DCMAKE_BUILD_TYPE=Release \
              "-DIris_DIR=$PWD/sdk/lib/cmake/Iris" \
              "-DQca3-qt6_DIR=$PWD/qca-sdk/lib/cmake/Qca3-qt6"
          else
            cmake -S consumer -B consumer-build \
              -DCMAKE_BUILD_TYPE=Release \
              "-DIris_DIR=$PWD/sdk/lib/cmake/Iris" \
              "-DQca3-qt6_DIR=$PWD/qca-sdk/lib/cmake/Qca3-qt6"
          fi
          cmake --build consumer-build --config Release --parallel

      - name: Run installed consumer on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:PATH = "$pwd\sdk\bin;$pwd\qca-sdk\bin;$env:QT_ROOT_DIR\bin;$env:PATH"
          & "$pwd\consumer-build\iris-consumer.exe"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Run installed consumer on macOS
        if: runner.os == 'macOS'
        shell: bash
        run: |
          DYLD_LIBRARY_PATH="$PWD/sdk/lib:$PWD/qca-sdk/lib:${DYLD_LIBRARY_PATH:-}" \
            ./consumer-build/iris-consumer

  android:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - abi: arm64-v8a
            qt_arch: android_arm64_v8a
          - abi: x86_64
            qt_arch: android_x86_64
          - abi: x86
            qt_arch: android_x86
    name: Android (${{ matrix.abi }})
    steps:
      - uses: actions/checkout@v4

      - name: Build and install Iris
        id: build-iris
        uses: ./.github/actions/build-iris-android
        with:
          qt-version: ${{ env.QT_VERSION }}
          qt-arch: ${{ matrix.qt_arch }}
          abi: ${{ matrix.abi }}
          ndk-version: ${{ env.ANDROID_NDK_VERSION }}
          android-api: ${{ env.ANDROID_API }}
          qca-tag: ${{ env.QCA_TAG }}
          install-prefix: ${{ github.workspace }}/sdk

      - name: Compile installed CMake consumer
        shell: bash
        env:
          ANDROID_NDK_ROOT: ${{ steps.build-iris.outputs.ndk-path }}
        run: |
          mkdir consumer
          cat > consumer/CMakeLists.txt <<'CMAKE'
          cmake_minimum_required(VERSION 3.16)
          project(iris_consumer LANGUAGES CXX)
          find_package(Iris CONFIG REQUIRED)
          add_library(iris-consumer SHARED main.cpp)
          target_link_libraries(iris-consumer PRIVATE Iris::Iris)
          CMAKE
          cat > consumer/main.cpp <<'CPP'
          #include <iris/jid/jid.h>
          extern "C" int iris_consumer_smoke()
          {
              XMPP::Jid jid(QStringLiteral("ci@example.org"));
              return jid.isValid() ? 0 : 1;
          }
          CPP
          qt_cmake=$(find "$QT_ROOT_DIR" -type f -name qt-cmake -print -quit)
          "$qt_cmake" -S consumer -B consumer-build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DANDROID_PLATFORM="android-$ANDROID_API" \
            "-DIris_DIR=$PWD/sdk/lib/cmake/Iris" \
            "-DQca3-qt6_DIR=$PWD/qca-sdk/lib/cmake/Qca3-qt6"
          cmake --build consumer-build --parallel
